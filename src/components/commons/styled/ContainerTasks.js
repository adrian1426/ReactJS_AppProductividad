import { styled } from '@mui/material/styles';

const ContainerTasks = styled('div')(({ theme }) => ({
  backgroundColor: theme.palette.common.black,
  display: 'flex',
  justifyContent: 'center'
}));

export default ContainerTasks;